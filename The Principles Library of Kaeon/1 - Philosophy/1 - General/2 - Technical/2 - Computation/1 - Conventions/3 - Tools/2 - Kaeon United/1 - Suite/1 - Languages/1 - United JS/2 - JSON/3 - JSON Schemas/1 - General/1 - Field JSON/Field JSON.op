Field JSON

	Philosophy

		-
			A field JSON object is a JSON object where every field value is a string.
		-

	Principles

		ID JSON

			-
				An ID JSON object is a JSON object where every field value is a number.
			-

		Switch JSON

			-
				A switch JSON object is a JSON object where every field value is a boolean.
			-